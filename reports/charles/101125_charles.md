# Séparation des fichiers mermaid

J'ai séparé les diagrammes mermaid en plusieurs fichiers.

J'ai tenté d'adopter une structure spéciale pour fusionner les diagrammes (séparés) en un seul diagramme mais ça ne donnait pas un bon résultat donc abandon

J'ai ajouté le diagramme pour décrire le système de filtrage (un prototype).

J'ai commencé à rechercher comment gérer les uploads (fichiers) avec flask.

J'ai trouvé une autre API (française) https://data.bnf.fr/fr/api 