# Mise en place de la sécurité

Mise en place du système de filtrage avec LLM + API.

Le système n'est pas pleinement fonctionnel, l'API retourne toujours que c'est copyrighté.

Le système écrit des logs pour le signaler aux administrateurs. L'aspect modération n'est pas encore implémentée. Il manque la séparation des droits.